# gitignore syntax
# - if name starts with slash, its relative to place where the .gitignore is (usually root of project)
# - if the name ends with slash, its a directory, otherwise it could be both a file or a directory

# ignore all directories called __pycache__ everywhere
__pycache__/

# ignore the build and dist directories generated at project root
# these are generated by pyinstaller
/build/
/dist/

# ignore pyinstaller generated spec file
/pomodoro.spec

# ignore NSIS generated installer
/install_pomodoro.exe